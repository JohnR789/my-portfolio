module.exports = {
  extends: [
    'next/core-web-vitals',          // Next.js-specific rules to ensure good performance and adherence to web standards
    'plugin:@typescript-eslint/recommended',  // TypeScript-specific linting rules for better code quality
  ],
  rules: {
    // Add any custom ESLint rules here if needed
  },
};

  